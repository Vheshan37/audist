import 'package:flutter/material.dart';

class NextCasesScreen extends StatelessWidget {
  const NextCasesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Next Cases Interface')),
    );
  }
}
